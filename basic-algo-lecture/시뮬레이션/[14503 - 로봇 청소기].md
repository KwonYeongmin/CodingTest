```cpp
#include <iostream>

using namespace std;

/*
현재 칸이 아직 청소되지 않은 경우, 현재 칸을 청소한다.
현재 칸의 주변
$4$칸 중 청소되지 않은 빈 칸이 없는 경우,
바라보는 방향을 유지한 채로 한 칸 후진할 수 있다면 한 칸 후진하고 1번으로 돌아간다.
바라보는 방향의 뒤쪽 칸이 벽이라 후진할 수 없다면 작동을 멈춘다.
현재 칸의 주변
$4$칸 중 청소되지 않은 빈 칸이 있는 경우,
반시계 방향으로
$90^\circ$ 회전한다.
바라보는 방향을 기준으로 앞쪽 칸이 청소되지 않은 빈 칸인 경우 한 칸 전진한다.
1번으로 돌아간다.
*/


// 이 값이  0인 경우 청소되지 않은 빈칸, 1인 경우 벽이 있는 것
struct Node 
{
    int x, y;
    Node() : x(0), y(0) {  }
    Node(int _x, int _y) : x(_x), y(_y) {  }
};

int board[52][52];
Node StartNode;
int startDir;

int dx[4] = { 0,0,-1,1 };
int dy[4] = { -1,1,0,0 };

void Simulation() 
{

}

int main()
{
    int N, M;

    cin >> N >> M;

    for (int i = 0; i < N; ++i) 
    {
        for (int j = 0; j < M; ++j)
        {
            cin >> board[i][j];
        }
    }

    cin >> StartNode.x >> StartNode.y >> startDir;

    return 0;
}
```
